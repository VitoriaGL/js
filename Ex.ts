function multiplicar(a: number, b: number): number {
    return a * b;
}

function saudacao(nome: string): string {
    return "Olá " + nome;
}

// Exemplos de uso:
console.log(multiplicar(2, 3)); // 6
console.log(saudacao("Vitória")); // Olá Vitória
